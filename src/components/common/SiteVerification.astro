---
import { SITE } from 'astrowind:config';
import { getSiteSettings } from '~/lib/siteSettings';

const id = getSiteSettings()?.googleSiteVerificationId || SITE.googleSiteVerificationId;
---

{id && <meta name="google-site-verification" content={id} />}
