# Copy this file to `.env` (never commit the real `.env`).
NODE_ENV=production

# Where the SQLite database file lives (a Docker volume mounts this path so
# data survives rebuilds — see docker-compose.yml).
DATABASE_PATH=/app/data/akort.db
